# LumaForge v2.1.17

## 功能优势
- 智能画布增加生成历史、资源 `@` 引用、引用与连线同步、创作助手仅整理模式、自动布局与撤销。
- 本地目录支持自定义，切换位置时可以选择只切换、复制旧文件或移动旧文件。
- Windows 桌面版的图片、视频、音频、素材 ZIP 和画布 ZIP 统一保存到设置中的输出目录。
- 保存成功会返回真实磁盘路径并提供“打开所在文件夹”；桌面保存失败不会再伪装成浏览器下载成功。
- Windows 发行包补齐 Sharp/libvips DLL，避免桌面端服务端图像处理运行库缺失。
- API 地址支持协议自动适配，常见 Key、地址、模型、能力和网络错误会给出下一步动作。

## 数据与兼容性
- 不修改云端数据库结构，不删除本地 `data/`、`userdata/`、`cloud-data/` 或素材目录。
- 桌面流式保存接口仅在 LumaForge 桌面运行态启用，源码模式和云端部署会拒绝该写入方式。
- 本次只构建 Windows 安装包和桌面 ZIP，不触发 macOS 构建。

## 验收重点
- 安装后版本显示 `2.1.17`。
- EXE 中下载图片、视频、音频和导出 ZIP 时，不出现 WebView 浏览器下载条。
- 保存结果显示真实路径，且“打开所在文件夹”可用。
- `http://127.0.0.1:3001` 源码模式保持浏览器保存兼容，桌面专用写入接口返回拒绝状态。
