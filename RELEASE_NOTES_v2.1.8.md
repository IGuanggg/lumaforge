# LumaForge v2.1.8 生图引用增强版

v2.1.8 聚焦画布生图的稳定性和参考图输入体验，让慢速接口更容易完成，让连接图片更自然地进入描述和生图请求。

## 功能优势

- 生图任务等待时间更长，适合耗时更久的图片模型，减少接口还在处理时前端提前报超时。
- 画布节点连接图片后，生成输入框会直接显示可点击的图片缩略图引用。
- 输入 `@` 可选择已连接图片，选中后描述中显示图片 token，提示词和参考图关系更清楚。
- 提交图生图时，连接图片会随请求作为参考图上传，不只是把图片名称写进文字里。
- 参考图识别会沿输入连线向上查找，复杂节点链路里的图片也更容易被生成节点识别。

## 验收重点

- 连接图片节点到生成节点后，输入框上方能看到图片缩略图。
- 点击缩略图或输入 `@` 选择图片后，描述里出现图片引用 token。
- 提交生图时，图片引用进入 `/images/edits` multipart 请求并作为 `image` 文件字段上传。
- 慢速图片接口可以等待更久，不再沿用普通聊天请求的短超时。

## 旧版本用户升级

- 2.1.7 及更早版本可以通过桌面端更新检查识别 2.1.8。
- 升级会保留本地登录、API 配置、画布、素材和数据目录。
- 如果旧版本已经连接了图片节点，升级后在新输入框中即可看到图片引用入口。
