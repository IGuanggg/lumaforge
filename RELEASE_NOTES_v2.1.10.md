# LumaForge v2.1.10 桌面更新热修版

v2.1.10 是桌面端热修版本，重点恢复设置页里的“重启应用 / 退出应用”，并修复更新下载进度容易卡住或状态不同步的问题。

## 功能优势

- Go + Next 桌面端可以正确识别本地桌面生命周期桥，设置页按钮不再被误禁用。
- 点击“重启应用”会交给桌面壳执行关闭和重新打开，升级后更容易完成收口。
- 点击“退出应用”会交给桌面壳退出当前应用进程，不再停留在无响应按钮状态。
- 更新进度会读取同一条桌面更新状态，下载不再出现前端看不到真实进度的状态割裂。
- 下载长时间无进度时会显示停滞状态，并提供清理半包后重试的入口。

## 验收重点

- 打开设置页后，“重启应用”和“退出应用”按钮应该可点击。
- 动作状态不再显示“Go + Next 主体暂未接入桌面生命周期控制”。
- 点击下载更新后，进度条、下载大小、SHA256 状态会持续刷新。
- 如果下载卡住，设置页会提示停滞并允许清理半包后重新下载。
- 如果不是桌面壳启动，页面仍会给出明确原因，避免误操作。

## 旧版本用户升级

- 2.1.9 及更早版本可以通过桌面端更新检查识别 2.1.10。
- 升级会保留本地登录、API 配置、画布、素材和数据目录。
- 更新后建议进入设置页确认“桌面端动作”和“自动更新”区域状态正常。
