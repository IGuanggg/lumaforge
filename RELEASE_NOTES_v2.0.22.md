# LumaForge v2.0.22 智能画布体验修复版

v2.0.22 是一个小版本稳定修复，重点优化智能画布的连线动效、节点输入框收起行为，以及成功出图后的错误状态清理。

## 更新内容

- 恢复智能画布连线的轻微流光动效，连线状态更清晰。
- 点击画布空白区域会收起节点输入框，并在收起前保存提示词草稿。
- 再次选中节点时，原提示词会完整恢复，不会因为点击空白丢失。
- 修复节点曾经生成失败后，即使后续成功出图仍然显示红框/失败徽标的问题。
- 有成功图片的节点会自动清理旧错误状态；真正失败且没有图片的节点仍保留错误提示。
- 保持本次更新为小版本修复，不新增复杂功能。

## 验收重点

- 智能画布连线能看到低调流动效果。
- 打开节点输入框后点击画布空白，输入框会收起。
- 输入提示词后点击空白再点回节点，文字仍然存在。
- 节点失败后重新成功出图，失败红框和失败徽标会消失。
- 没有成功图片的失败节点仍正常显示错误提示。

## 构建产物

| 文件 | 说明 | 大小 |
|------|------|------|
| `LumaForge-2.0.22-desktop.zip` | 桌面版，含 EXE + 自动更新器 | ~50 MB |
| `LumaForge-Setup-2.0.22.exe` | Windows 一键安装器 | ~40 MB |
| `lumaforge-browser-v2.0.22.zip` | 浏览器/源码运行包 | 2.6 MB |

## SHA256

```
LumaForge-2.0.22-desktop.zip
1ECD2A7B05B88B299AD721B008F48242B12607DC1A41970487A09521CF36D347

LumaForge-Setup-2.0.22.exe
55EF1020C739D478D62400DF296ED1B61F79642DF5F88A43FCCAEB9C0903DB7D

lumaforge-browser-v2.0.22.zip
79533B910C365A54DFD6E49ED7B86BEA951B5504A5F4084D64C92EA3BF1A25AC
```

## macOS

macOS 包需要在 macOS 环境或 GitHub Actions macOS runner 上构建，详见 README 中的 macOS 构建说明。

## 注意

- Windows 未签名 EXE 可能触发 SmartScreen 提示。
- 安装器卸载不删除用户数据目录。
- 自动更新依赖 Release 中的 zip 资产。
