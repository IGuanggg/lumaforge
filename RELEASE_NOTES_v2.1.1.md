# LumaForge v2.1.1 画布体验增强版

v2.1.1 聚焦无限画布创作体验，让图像生成、素材组织、模型选择和多平台 API 能更自然地串在同一个工作流里。

## 功能亮点

- 无限画布支持更顺手的节点创作、连接、缩放和定位，适合把灵感、参考图、生成结果和二次编辑放在同一个空间里连续推进。
- 画布节点可以承载更完整的图像设置和素材引用，适合多版本对比、局部探索、风格延展和成组生成。
- 新增更细的画布图片操作体验，包括快速工具、网格切分和更稳定的节点图片数据处理。
- API 设置与用户偏好统一走本地 API 平台数据，已添加的平台、模型和 Key 状态可以在配置里清楚查看。
- 云端渠道和本地 API 平台分开呈现，云端未配置时不会混入本地平台，也不会覆盖本地配置。
- 登录状态在本地访问地址之间更稳定，减少因为 localhost/127.0.0.1 切换造成的重复登录。
- 素材库与画布素材引用更稳，旧素材、历史输出和本地文件路径可以继续服务新画布流程。

## 优势

- 创作链路更集中：从选模型、放参考、生成、拆图、对比到继续扩展，都能留在画布里完成。
- 多平台更清楚：ModelScope、百炼、grsai、Gemini、GPT 等本地 API 平台各自独立，模型选择能按平台精确路由。
- 数据更安心：旧版本用户升级时，已有画布、素材、API 设置、云端账户和本地 Key 会继续保留。
- 成本更可控：本地 API 平台优先走用户自己的配置，云端渠道作为独立能力保留，不会误用或覆盖。
- 桌面体验更稳：启动、配置同步、素材恢复和更新检查都围绕长时间创作场景优化。

## 旧版本用户升级

- 升级不会清空已有画布、素材、API 设置、云端账户或本地 Key。
- 首次进入新版画布后，可以继续使用原有素材和输出记录。
- 建议升级前关闭正在运行的旧版 LumaForge，安装完成后再启动新版。
