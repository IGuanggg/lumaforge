# LumaForge v2.1.6 登录状态稳定版

v2.1.6 聚焦桌面端登录保持能力，让重启、升级、刷新和入口切换后的账户状态更稳定。

## 功能亮点

- 登录成功后会写入同域 cookie 作为兜底凭证。
- 页面没有读到本地 token 时，会自动向后端尝试恢复当前账户。
- 桌面端本地认证密钥会保存到数据目录，重启和升级后继续复用。
- 退出登录会同步清理后端云端会话和 cookie。

## 优势

- 减少桌面端重启后反复要求登录的情况。
- 减少 `localhost`、`127.0.0.1` 或端口变化造成的登录状态割裂。
- 前端和后端账户状态更一致，手动重新登录的次数更少。
