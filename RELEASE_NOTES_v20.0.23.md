# LumaForge v20.0.23 智能画布交互修复版

v20.0.23 是智能画布小版本稳定修复，重点优化鼠标操作、缩放体验、输入框比例、连线动效和成功出图后的状态清理。

## 更新内容

- 统一智能画布鼠标操作：左键选择/框选，中键拖动画布，右键打开属性，Alt+滚轮缩放，Ctrl+A 全选节点。
- 移除旧的空格+左键拖动画布和普通滚轮缩放逻辑。
- 缩放逻辑参考无限画布，使用稳定步进（放大 1.08 / 缩小 0.92）和缩放上下限（0.12x ~ 3x），以鼠标位置为锚点。
- 画布缩放时，图片节点按画布比例缩放，生成文本框保持屏幕可读性，并会在缩放后自动刷新。
- 修复文本框比例需要重新点击节点才刷新的问题。
- 智能画布连线动效同步无限画布：选中相关节点时显示双层流光和沿线移动光点。
- 连线命中区域保持更灵敏，删除按钮逻辑继续保留。
- 修复节点成功出图后仍显示失败红框/失败徽标的问题。
- README 和交接文档同步新的智能画布操作说明。

## 验收重点

- 左键拖空白区域可以框选节点，不会拖动画布。
- 按住中键可以拖动画布。
- 普通滚轮不缩放，Alt+滚轮才缩放。
- Ctrl+A 可以全选节点。
- 缩放后文本框会自动刷新，不需要重新点击节点。
- 缩小时图片框会变小，但生成文本框仍保持可读。
- 选中有连接的节点后，连线出现无限画布同款流光和运动光点。
- 成功出图的节点不再保留失败红框或失败徽标。

## 构建产物

| 文件 | 说明 | 大小 |
|------|------|------|
| `LumaForge-20.0.23-desktop.zip` | 桌面版，含 EXE + 自动更新器 | ~50 MB |
| `LumaForge-Setup-20.0.23.exe` | Windows 一键安装器 | ~40 MB |
| `lumaforge-browser-v20.0.23.zip` | 浏览器/源码运行包 | 2.6 MB |

## SHA256

```
LumaForge-20.0.23-desktop.zip
E6B7A62C3C82851BC31961E4C9F98B9854340599AA74DE50FCCC0DEBCA964CBE

LumaForge-Setup-20.0.23.exe
5EC1F37B270F2B88F8A5CE2A5347B8D01310F2F029E7CFE3A578CFA21B213514

lumaforge-browser-v20.0.23.zip
A0E64AA69379E11EE74D9578A1BF639A8956355DFB993BF3ACE665A365F4E8DD
```

## macOS

macOS 包需要在 macOS 环境或 GitHub Actions macOS runner 上构建，详见 README 中的 macOS 构建说明。

## 注意

- Windows 未签名 EXE 可能触发 SmartScreen 提示。
- 安装器卸载不删除用户数据目录。
- 自动更新依赖 Release 中的 zip 资产。
