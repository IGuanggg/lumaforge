# LumaForge v2.1.3 桌面自动更新热修版

v2.1.3 聚焦桌面端自动更新体验，让旧版本在发现新版本后可以更稳定地完成下载、替换和重新启动。

## 功能亮点

- 桌面端自动更新会在安装前更可靠地关闭旧版本残留进程，减少下载完成但更新失败的情况。
- 更新完成后会自动尝试重新启动 LumaForge，用户不需要反复手动关闭和打开程序。
- 更新过程继续保护画布、素材、API 设置、账号状态、缓存、日志和本地数据目录，升级不会覆盖个人创作数据。
- 更新状态记录更清晰，失败时可以看到替换、回滚或重启阶段的状态，便于快速判断下一步操作。
- 旧版本用户可以通过内置更新检查识别 v2.1.3，适合从 2.1.1/2.1.2 紧急升级。

## 优势

- 升级更省心：自动更新流程覆盖检查、下载、安装和重启，减少手动处理安装包的次数。
- 桌面体验更稳定：旧进程退出不完整时也能继续完成更新，降低文件占用导致的失败概率。
- 数据更安心：用户的画布、素材和配置目录继续被保护，升级重点只替换程序本体。
- 排障更直接：更新过程的状态更明确，遇到异常时可以更快定位是下载、安装还是重启阶段。

## 旧版本用户升级

- 可以在桌面端设置页使用检查更新识别 v2.1.3。
- 升级前建议等待正在生成的任务完成，避免中途打断正在写入的素材。
- 如自动重启被系统拦截，可手动重新打开 LumaForge，程序文件和个人数据会继续保留。
