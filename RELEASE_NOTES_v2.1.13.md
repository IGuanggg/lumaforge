# LumaForge v2.1.13

## 功能优势

- 修复 Nano Banana 出图链路，`grsai / nano-banana*` 模型会走稳定的专用生图接口，参考图、比例和尺寸参数能正确传递。
- 本地生图失败时会返回更明确的上游错误信息，便于判断是接口、模型还是参数问题。
- API 设置页支持更稳定地手动添加模型，新增空白模型输入不会立刻消失，保存后会自动去空、去重和整理。
- 模型列表拉取增加追加/覆盖选择，默认追加，避免手填模型被上游列表覆盖。
- API 检测能区分真实连接成功和手动模型兜底；上游模型列表不可用时显示黄色提醒，不再误报绿色通过。
- 增加平台显示名修复能力，可把历史保存的乱码平台名恢复成可读名称，同时保留 Key、Base URL 和模型配置。
- 画布节点工具栏点击更稳定，悬浮按钮不会被画布事件吞掉。
- 支持在节点上使用 `Ctrl/Cmd + 滚轮` 单独缩放节点，减少误触整体画布缩放。
